export GPG_TTY=$TTY
export POWERLEVEL9K_CONFIG_FILE=${XDG_CONFIG_HOME:-$HOME/.config}/p10k/p10k.zsh
[[ -t 0 ]] && stty -ixon

# Enable Powerlevel10k instant prompt. Should stay close to the top of ~/.zshrc.
# Initialization code that may require console input (password prompts, [y/n]
# confirmations, etc.) must go above this block; everything else may go below.
if [[ -n "$TMUX" && -z $alfred_version && -r "${XDG_CACHE_HOME:-$HOME/.cache}/p10k-instant-prompt-${(%):-%n}.zsh" ]]; then
  source "${XDG_CACHE_HOME:-$HOME/.cache}/p10k-instant-prompt-${(%):-%n}.zsh"
fi

# auto-correction.
CORRECT_IGNORE_FILE="go|.build|.ssh|.config"
alias cp='nocorrect cp'
alias man='nocorrect man'
alias mkdir='nocorrect mkdir'
alias mv='nocorrect mv'
alias sudo='nocorrect sudo'
alias su='nocorrect su'
alias glk='nocorrect gitleaks'
setopt correct_all

# history
HISTFILE=${ZDOTDIR}/history
HISTCONTROL=ignoreboth
setopt extended_history       # record timestamp of command in HISTFILE
setopt hist_expire_dups_first # delete duplicates first when HISTFILE size exceeds HISTSIZE

export VISUAL="nvim"
export EDITOR="nvim"
export PAGER=bat
export DELTA_PAGER=$PAGER
export MANPAGER="sh -c \"col -bx | $PAGER -l man --paging always\""

# compilation flags
export ARCHFLAGS=$(uname -m)

# >>> zim >>>
ZIM_CONFIG_FILE=${XDG_CONFIG_HOME}/zsh/zimrc
ZIM_HOME=${XDG_CONFIG_HOME}/zim

if [[ ! -e ${ZIM_HOME}/zimfw.zsh ]]; then
  mkdir -p ${ZIM_HOME}
  brew install zimfw
  ln -s $(brew --prefix zimfw)/share/zimfw.zsh ${ZIM_HOME}/
fi

if [[ ! ${ZIM_HOME}/init.zsh -nt ${ZIM_CONFIG_FILE:-${ZDOTDIR:-${HOME}}/.zimrc} ]]; then
  source ${ZIM_HOME}/zimfw.zsh init -q
fi

source $ZIM_HOME/init.zsh
# <<< zim <<<

# To customize prompt, run `p10k configure` or edit $POWERLEVEL9K_CONFIG_FILE.
[[ -f $POWERLEVEL9K_CONFIG_FILE && -z $alfred_version ]] && source $POWERLEVEL9K_CONFIG_FILE

source $ZDOTDIR/config.zsh
