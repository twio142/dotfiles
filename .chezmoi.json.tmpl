{{ $color := promptStringOnce . "color" "What is your tmux theme color" -}}
{
  "env": {
    "TMUX_THEME_COLOR": "{{ $color }}"
  },
  "hooks": {
    "apply": {
      "pre": {
        "command": "ya",
        "args": ["pkg", "upgrade"]
      }
    }
  }
}
